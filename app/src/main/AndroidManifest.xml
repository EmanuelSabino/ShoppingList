<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppinglist">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".about"></activity>
        <activity android:name=".settings" />
        <activity android:name=".ui.List.EditList.EditOneProduct" />
        <activity android:name=".ui.Categories.EditCategory.EditOneCategory" />
        <activity android:name=".ui.Categories.EditCategory.edit_category" />
        <activity android:name=".ui.List.EditUrgenList.edit_urgent_list" />
        <activity android:name=".welcome01layout" />
        <activity android:name=".ui.Product.AddNewProduct.AddNewProduct" />
        <activity android:name=".ui.Categories.AddNewCategories.AddNewCategory" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>